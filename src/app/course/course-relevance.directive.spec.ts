import { CourseRelevanceDirective } from './course-relevance.directive';

describe('CourseRelevanceDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseRelevanceDirective();
    expect(directive).toBeTruthy();
  });
});
